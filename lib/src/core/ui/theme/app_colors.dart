import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color seed = Color(0xFF2E7D32); // Forest Green (M3 seed)
  static const Color primary = Color(0xFF2E7D32); // Forest Green
  static const Color fresh = Color(0xFF43A047); // Fresh Green (gradient phụ)
  static const Color mint = Color(0xFFA5D6A7); // Mint (nền decorative)

  // Background
  static const Color bg = Color(0xFFF1F8F4); // Nền app chính
  static const Color warm = Color(0xFFFFFDF7); // Nền ấm (top gradient)

  // Surface
  static const Color surface = Color(0xEBFFFFFF); // rgba(255,255,255,0.92)
  static const Color surfaceStrong = Color(0xFFFFFFFF); // Trắng đặc

  // Text
  static const Color text = Color(0xFF102A16); // Chữ chính
  static const Color muted = Color(0xFF6B7B6E); // Chữ phụ

  // Border
  static const Color border = Color(0x14102A16); // rgba(16,42,22,0.08)

  // Macro nutrients
  static const Color protein = Color(0xFF1E88E5); // Vivid Blue
  static const Color carb = Color(0xFFFB8C00); // Warm Amber
  static const Color fat = Color(0xFFE91E63); // Rose Pink

  // Feature accents
  static const Color water = Color(0xFF039BE5); // Ocean Blue
  static const Color streak = Color(0xFFFF6D00); // Fire Orange

  // AI
  static const Color ai = Color(0xFF7C4DFF); // AI accent tím
  static const Color aiSoft = Color(0xFFF0EBFF); // AI nền nhạt

  // Premium
  static const Color gold = Color(0xFFF9A825); // Premium accent

  // Semantic
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color danger = Color(0xFFDC2626); // Đỏ xoá/lỗi (CSS: #DC2626)

  AppColors._();
}
